import * as React from 'react';
import markerInterval from './marker-interval.png';

function MarkerInterval() {
  return (
    <div className="markers">
      <img src={markerInterval} />
    </div>
  );
}

export default MarkerInterval;
